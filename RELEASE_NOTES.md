# 秋水桥梁 v1.4.8 升级说明

## 本次更新

**修复 v1.4.7 已登录用户打开客户端全黑的严重问题。**

如果你安装了 v1.4.7 后打开是黑屏，请从下载站重新下载 v1.4.8 覆盖安装即可恢复。
黑屏状态下应用内的「检查更新」也无法使用，必须手动重新下载。

v1.4.7 的新功能在本版本中全部保留：

- 实时流量统计：状态卡显示上下行速率与本次连接用量，可在设置里关闭。
- 连接时长显示，切换节点后重新计时。
- 节点支持收藏置顶，新增「全部测速」批量检测（带进度，可随时停止）。
- 界面主题新增「跟随系统」，随系统深浅色实时切换。
- 补齐键盘操作与读屏支持：节点卡可用键盘选择，连接状态变化会被读屏播报。
- 尊重系统「减少动态效果」与「提高对比度」偏好。
- 后台 v1.5.1：补充容器日志轮转，修正部署包打包流程。

## 关于 v1.4.7 的问题

两个新增的 React Hook 被放在了组件的提前 return 之后，导致「未登录」与
「已登录」两次渲染的 Hook 数量不一致，React 抛出 #310 错误并卸载整个界面，
表现为全黑窗口。未登录用户不受影响，因此发布前的验证没有覆盖到。

本版本已修复，并新增回归测试锁定该问题：测试会真实复现 Hook 数量不一致的
崩溃，并断言两个 Hook 的位置早于所有提前 return。

v1.4.7 的服务端发布记录已全部下线，不会再有用户收到该版本。

## 升级方式

- Windows：运行 `Setup.1.4.8.exe` 覆盖安装。
- Android：安装 `qiushui-bridge-1.4.8-universal.apk`，可直接覆盖旧版本。
- macOS：按芯片型号选择相应的 DMG 或 ZIP 包安装。
- 服务端：无需变更，继续使用 v1.5.1。

## 关于实时流量统计

该功能通过 sing-box 的本机管理接口读取速率，实现上有三条硬约束：

- 接口只监听 `127.0.0.1`，不对外网开放；
- 访问密钥每次连接随机生成，不保存到磁盘、不跨连接复用；
- 不启用任何可交互的网页控制面板。

如不需要，可在「设置 → 通用 → 实时流量统计」关闭，下次连接生效。

## 验证状态

- 桌面端自动化测试：58/58 通过（含 3 个针对本次问题的回归测试）。
- 服务端自动化测试：54/54 通过。
- TypeScript、语法检查与前端构建全部通过。
- 已用带登录态的用户数据实际渲染验证：界面完整显示，无 React 错误。
  同一份数据下 v1.4.7 的代码渲染结果为空（即黑屏），v1.4.8 正常。

## 注意事项

- Windows 与 macOS 安装包尚未配置商业平台签名，请按系统安全提示确认来源，
  并核对随包提供的 SHA256。
- 不要替换 Android 签名证书，否则已安装旧版本的设备无法直接覆盖升级。
- 本次更新为非强制更新。
