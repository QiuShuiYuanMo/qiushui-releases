# 秋水桥梁 v1.4.7 升级说明

## 本次更新

- 新增实时流量统计：状态卡显示上下行速率与本次连接用量，可在设置里关闭。
- 新增连接时长显示，切换节点后重新计时。
- 节点支持收藏置顶，新增"全部测速"批量检测（带进度，可随时停止）。
- 主题新增"跟随系统"，随系统深浅色实时切换。
- 补齐键盘操作与读屏支持：节点卡可用键盘选择，连接状态变化会被读屏播报。
- 尊重系统"减少动态效果"与"提高对比度"偏好。
- 安装包内固化构建来源，诊断报告可自证是 CI 构建还是本地构建。
- 后台 1.5.1：补充容器日志轮转，修正部署包打包流程。

## 升级方式

- Windows：运行 `Setup.1.4.7.exe` 覆盖安装。
- Android：安装 `qiushui-bridge-1.4.7-universal.apk`，可直接覆盖 v1.4.6。
- macOS：按芯片型号选择相应的 DMG 或 ZIP 包安装。
- 服务端：使用 `qsb-server-v1.5.1.zip` 部署，并执行 Prisma 数据库迁移
  （本次无 schema 变更，为空操作）。

## 关于实时流量统计

该功能通过 sing-box 的本机管理接口读取速率，实现上有三条硬约束：

- 接口只监听 `127.0.0.1`，不对外网开放；
- 访问密钥每次连接随机生成，不保存到磁盘、不跨连接复用；
- 不启用任何可交互的网页控制面板。

如不需要，可在"设置 → 通用 → 实时流量统计"关闭，下次连接生效。
关闭后客户端不会启用该接口。

## 验证状态

- 桌面端自动化测试：55/55 通过。
- 服务端自动化测试：54/54 通过。
- TypeScript、语法检查与前端构建全部通过。
- 真实 Electron 启动确认 42 个进程间通道全部正常注册。
- 真实 sing-box 进程确认流量统计链路可用。

## 注意事项

- Windows 与 macOS 安装包尚未配置商业平台签名，请按系统安全提示确认来源，
  并核对随包提供的 SHA256。
- 不要替换 Android 签名证书，否则已安装旧版本的设备无法直接覆盖升级。
- 本次更新为非强制更新。
